<main class="main">
  <div class="inner">
    <%if user_signed_in? %>
      <div class="greeting">
       <%= link_to "#{current_user.name}マイページ", user_path(current_user) , class: :greeting__link%> 
      </div>
     <% end %>
    <div class="card__wrapper">
    <div class="user__card">
      <%= render partial: 'photo', collection: @photos %>
    </div>
    </div>
  </div>
</main>
